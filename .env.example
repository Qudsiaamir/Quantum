# Optional: required only for the D-Wave feature-selection section.
# Use these names as a shell-export template before starting Jupyter.
# The notebook does not auto-load this file.

DWAVE_API_TOKEN=
DWAVE_API_ENDPOINT=https://cloud.dwavesys.com/sapi/
DWAVE_API_SOLVER=
